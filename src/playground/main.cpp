#include <vector>
#include <string>
#include <fstream>

// #include "yaml-cpp/yaml.h"

int main()
{
    // std::vector<std::string> images{
    //     "test.jpg",
    //     "alpha.png",
    // };

    // YAML::Emitter out;
    // out << YAML::BeginMap;
    // out << YAML::Key << "Images";
    // out << YAML::BeginSeq;
    // for (auto image: images)
    // {
    //     out << image;
    // }
    // out << YAML::EndSeq;
    // out << YAML::EndMap;

    // std::ofstream fout("test.yaml");
    // fout << out.c_str();
}

